--- ../src/Gateway/Mistral/MistralGateway.php
+++ ../src/Gateway/Mistral/MistralGateway.php
@@ -179,6 +179,9 @@
                 ->post('audio/transcriptions', array_filter([
                     'model' => $model,
                     'language' => $language,
+                    'diarize' => $diarize,
+                    'timestamp_granularities' => $diarize ? ['segment'] : null,
+                    'response_format' => $diarize ? 'json' : null,
                 ])),
         );
 
@@ -188,7 +191,7 @@
             $data['text'] ?? '',
             collect($data['segments'] ?? [])->map(fn (array $segment) => new TranscriptionSegment(
                 $segment['text'] ?? '',
-                $segment['speaker'] ?? '',
+                $segment['speaker_id'] ?? '',
                 $segment['start'] ?? 0,
                 $segment['end'] ?? 0,
             )),
